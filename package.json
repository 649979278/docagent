{
  "name": "workagent",
  "version": "0.1.0",
  "private": true,
  "description": "离线公文写作Agent - Windows桌面应用",
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "packageManager": "pnpm@9.15.0",
  "devDependencies": {
    "@types/node": "^22.0.0"
  }
}
