## 目标

修复桌面端本轮 7 个 UI/交互问题，并验证计划模式、权限策略、模型选择在真实应用中有效。

## 范围

1. 调整标题栏、侧栏、对话区的桌面布局与对齐。
2. 修复输入框初始滚动条、输入区上下比例、浮层遮挡和按钮错位。
3. 修复项目下会话删除/归档后的跳转与恢复链路。
4. 补强项目更多菜单、归档恢复、计划模式显示与环境浮窗信息。
5. 启动桌面端并做一轮真实交互验证。

## 约束

- 不改系统级网络环境。
- 不做硬编码视觉兜底，沿用现有 stores / ipc / runtime 链路。
- 真实验证以本地 Electron 桌面端为准。
