WorkAgent 三期核心闭环 + RAG + 测试 落地计划

 Context

 WorkAgent 三期计划完成度约 35%。核心断裂点：
 1. Plan → Execute 未闭环（runtime 未调用 startExecution）
 2. 检索流水线空壳（sparse/rerank/rewrite 全为 placeholder）
 3. 运行时恢复缺 drain collapse + transcript 只写不用
 4. IPC 未拆分（640 行单文件）、Plan 审批 UI 缺失、abort 事件断裂
 5. ingest/rag/tools 包无 test 脚本、fixtures 不存在

 本计划按"闭环优先"原则，保证核心运行时完美闭环后升级 RAG，最后补恢复能力和分层验收。

 ---
 Task 1: 收口环境、包脚本和打包链路

 1.1 给 ingest/rag/tools 添加 test 脚本

 文件: packages/ingest/package.json / packages/rag/package.json / packages/tools/package.json

 各加一行 "test": "vitest run"。vitest.config.ts 已存在，无需创建。

 1.2 模型常量（无构建依赖，doctor-env 可直接引用）

 新建: packages/shared/model-constants.cjs

 此文件为纯 CJS，不经过 TypeScript 编译，doctor-env.js 在构建前也能 require()：

 'use strict';

 /**
  * Ollama 必需模型清单（精确匹配模型名）。
  * 此文件不依赖构建流程，doctor-env.js 在 dist/ 不存在时也能引用。
  */
 const REQUIRED_OLLAMA_MODELS = {
   chat: 'qwen3.5:9b',
   embedding: 'bge-m3',
   reranker: 'bge-reranker-v2-m3',
 };

 /**
  * 模型名 alias 白名单。
  * key = 期望模型名，value = 可接受的替代 tag 列表。
  * 不在白名单中的变体一律视为不匹配。
  */
 const MODEL_ALIASES = {
   'qwen3.5:9b': ['qwen3.5:9b-q4_K_M'],
   'bge-m3': [],
   'bge-reranker-v2-m3': [],
 };

 /**
  * 检查 Ollama 模型列表是否包含指定模型。
  * 精确匹配优先，再查 alias 白名单。
  * qwen3.5:7b 不会误判为 qwen3.5:9b。
  */
 function isModelAvailable(modelName, availableModels) {
   // 精确匹配
   if (availableModels.includes(modelName)) return true;
   // alias 白名单匹配
   const aliases = MODEL_ALIASES[modelName] ?? [];
   return aliases.some(alias => availableModels.includes(alias));
 }

 module.exports = { REQUIRED_OLLAMA_MODELS, MODEL_ALIASES, isModelAvailable };

 文件: packages/shared/src/constants.ts — 同步定义 TS 版本：

 /** Ollama 必需模型清单（精确匹配模型名） */
 export const REQUIRED_OLLAMA_MODELS = {
   chat: 'qwen3.5:9b',
   embedding: 'bge-m3',
   reranker: 'bge-reranker-v2-m3',
 } as const;

 /**
  * 模型名 alias 白名单。
  * key = 期望模型名，value = 可接受的替代 tag 列表。
  */
 export const MODEL_ALIASES: Record<string, string[]> = {
   'qwen3.5:9b': ['qwen3.5:9b-q4_K_M'],
   'bge-m3': [],
   'bge-reranker-v2-m3': [],
 };

 /**
  * 检查 Ollama 模型列表是否包含指定模型。
  * 精确匹配优先，再查 alias 白名单。
  * qwen3.5:7b 不会误判为 qwen3.5:9b。
  */
 export function isModelAvailable(modelName: string, availableModels: string[]): boolean {
   if (availableModels.includes(modelName)) return true;
   const aliases = MODEL_ALIASES[modelName] ?? [];
   return aliases.some(alias => availableModels.includes(alias));
 }

 1.3 doctor-env.js 增加模型检查（引用 CJS 常量，不依赖构建产物）

 文件: scripts/doctor-env.js

 const { REQUIRED_OLLAMA_MODELS, isModelAvailable } = require('../packages/shared/model-constants.cjs');

 // ... 现有检查 ...

 // 检查模型存在性
 const tagsResp = await fetch('http://localhost:11434/api/tags');
 const tags = JSON.parse(await tagsResp.text());
 const modelNames = tags.models.map(m => m.name);
 let modelOk = true;
 for (const [key, required] of Object.entries(REQUIRED_OLLAMA_MODELS)) {
   const found = isModelAvailable(required, modelNames);
   console.log(`model_${key}: ${found ? 'ok' : 'MISSING → ollama pull ' + required}`);
   if (!found) modelOk = false;
 }
 if (!modelOk) process.exit(1);

 1.4 pnpm.overrides 锁 @noble/hashes

 文件: package.json

 "pnpm": {
   "overrides": {
     "@noble/hashes": "^1.8.0"
   }
 }

 验收

 - pnpm doctor:env — 全绿（含模型精确检查，不依赖 dist/）
 - pnpm test:unit — agent-core(30) + store(1) + ingest(6) + rag(4) + tools(4) 全通过
 - pnpm --filter @workagent/desktop build — 无 @noble/hashes ESM 报错

 ---
 Task 2: 修复 Plan → Execute、Abort 和审批 UI 闭环

 2.1 PlanModeController 多 listener 支持（防泄漏）

 文件: packages/agent-core/src/plan-controller.ts

 改为 Set 多 listener，onEvent() 返回 unsubscribe：

 private callbacks = new Set<PlanControllerCallback>();

 /**
  * 注册计划控制器事件监听器。
  * @param callback - 事件回调函数。
  * @returns 取消监听函数。
  */
 onEvent(callback: PlanControllerCallback): () => void {
   this.callbacks.add(callback);
   return () => this.callbacks.delete(callback);
 }

 // 所有 this.callback?.(event) 改为
 private emit(event: PlanControllerEvent): void {
   for (const cb of this.callbacks) { cb(event); }
 }

 ⚠️ 防泄漏关键：runtime.runTurn() 内如需注册 listener，必须保存 unsubscribe 引用，在 finally 中调用：

 // runtime.ts 中
 let planUnsub: (() => void) | null = null;
 try {
   planUnsub = this.planController.onEvent((e) => { ... });
   // ... runTurn 主逻辑 ...
 } finally {
   planUnsub?.();
 }

 2.2 runtime.ts 中 Plan 执行启动（产品语义：approved → next runTurn → executing）

 文件: packages/agent-core/src/runtime.ts

 产品语义明确：approvePlan() 将状态设为 approved；下一次 runTurn() 检测到 activePlan.status === 'approved' 时调用 startExecution()，状态变为 executing。前端可短暂看到 approved 状态。

 // 在 runTurn 循环开始处，检测 approved plan 并启动执行
 if (state.activePlan?.status === 'approved' && state.mode === 'execute') {
   this.planController.startExecution();
   state = updateQueryLoopState(state, {
     activePlan: { ...state.activePlan, status: 'executing' },
   });
 }

 2.3 Worker abort 发送 run_status:aborted（需要保存 currentSessionId/currentRunId）

 文件: apps/desktop/electron/workers/agent-worker.ts

 ⚠️ 硬阻塞：当前 agent-worker.ts 没有 currentSessionId/currentRunId 变量。必须：

 // 在文件顶部添加状态变量
 let currentSessionId: string | null = null;
 let currentRunId: string | null = null;

 // handleChat() 开始时保存
 async function handleChat(message: string, sessionId: string, mode?: string) {
   currentSessionId = sessionId;
   currentRunId = `run_${Date.now()}`;
   try {
     // ... 现有逻辑 ...
   } finally {
     currentSessionId = null;
     currentRunId = null;
   }
 }

 // abort handler 中使用这些变量
 if (currentIterator) {
   // 先发 run_status:aborted 事件
   parentPort?.postMessage({
     type: 'event',
     event: {
       sessionId: currentSessionId ?? '',
       turnId: '',
       sequence: Date.now(),
       type: 'run_status',
       data: {
         runId: currentRunId ?? '',
         status: 'aborted',
         terminalReason: 'aborted_streaming',
       },
       createdAt: Date.now(),
     },
   } as WorkerResponse);

   currentIterator.return?.(undefined);
 }

 2.4 handleAbort 防止重复 terminal event（按 runId 维护）

 文件: apps/desktop/electron/ipc-handlers.ts

 ⚠️ 修正：不能用单个 abortEmitted 布尔值在 handleAbort 末尾重置，否则并发 abort 或 worker 回传终止事件仍可能重复。改为按 runId 维护 terminal 状态：

 /** 已发出 terminal 事件的 runId 集合 */
 private terminalRunIds = new Set<string>();

 private async handleAbort(): Promise<{ success: boolean }> {
   const runId = this.currentRunId;
   if (!runId || this.terminalRunIds.has(runId)) {
     return { success: false };
   }
   this.terminalRunIds.add(runId);

   // ... 现有 abort 逻辑 ...

   return { success: true };
 }

 // 新 run 开始时清理旧 runId
 private startNewRun(runId: string): void {
   this.currentRunId = runId;
   // 保留最近的 terminalRunIds，防止过期累积
   if (this.terminalRunIds.size > 100) {
     this.terminalRunIds.clear();
   }
 }

 2.5 ConversationPane 添加 Plan 审批按钮（从 sessionStore 取 sessionId）

 文件: apps/desktop/src/components/ConversationPane.tsx

 import { useRunStore } from '../stores/run-store';
 import { useSessionStore } from '../stores/session-store';

 function PlanApprovalBanner(): React.ReactElement | null {
   const { planPhase, diagnostics, mode } = useRunStore();
   const currentSessionId = useSessionStore((s) => s.currentSessionId);
   const api = window.workagent;

   if (planPhase !== 'PLAN_REVIEW' || !diagnostics.activePlanId || mode !== 'plan') {
     return null;
   }

   return (
     <div className="flex items-center gap-2 px-4 py-2 bg-amber-50 border-b border-amber-200">
       <span className="text-sm text-amber-800">计划已生成，等待审批</span>
       <button
         className="px-3 py-1 text-sm bg-emerald-500 text-white rounded hover:bg-emerald-600"
         onClick={() => api.approvePlan(diagnostics.activePlanId!, true, currentSessionId ?? '')}
       >
         批准计划
       </button>
       <button
         className="px-3 py-1 text-sm bg-red-500 text-white rounded hover:bg-red-600"
         onClick={() => api.approvePlan(diagnostics.activePlanId!, false, currentSessionId ?? '')}
       >
         拒绝计划
       </button>
     </div>
   );
 }

 2.6 测试更新

 文件: tests/e2e/plan-approval-flow.test.ts

 it('approvePlan sets approved; next runTurn with mode=execute calls startExecution', async () => {
   const bundle = await createRuntimeBundleForTest('direct');
   const controller = bundle.runtime.getPlanController();

   controller.enterPlanMode('session-1');
   expect(controller.getActivePlan()?.status).toBe('draft');

   controller.approvePlan();
   expect(controller.getActivePlan()?.status).toBe('approved');
   expect(controller.getMode()).toBe('execute');

   // runtime 检测到 approved + mode=execute 后调用 startExecution
   // （此处直接调用模拟 runtime 行为）
   controller.startExecution();
   expect(controller.getActivePlan()?.status).toBe('executing');
   expect(controller.getPhase()).toBe('EXECUTE_DRAFT');
 });

 文件: tests/e2e/runtime-abort.test.ts

 it('worker abort emits run_status:aborted exactly once with correct runId', async () => {
   // 触发 chat → 记录 runId → abort → 收集事件
   // 断言恰好 1 个 run_status:aborted 且 runId 匹配
 });

 验收

 - approvePlan → status: approved（前端可短暂看到）
 - 下次 runTurn 检测到 approved+execute → startExecution → status: executing
 - Worker abort 发出 run_status:aborted 且 runId/sessionId 正确
 - terminalRunIds 防重复，新 run 开始时清理
 - onEvent 返回 unsubscribe，runTurn finally 中调用防泄漏

 ---
 Task 3: 拆分 IPC 领域处理器

 （与上一版相同，不涉及用户指出的修正点，此处省略重复。保持：context.ts / chat-ipc.ts / knowledge-ipc.ts / settings-ipc.ts 四文件拆分方案。）

 补充: knowledge-remove 明确语义 —— 只删除数据库记录和向量索引，不删除用户磁盘上的原始文件：

 ipcMain.handle('knowledge-remove', async (_ev, filePathOrDocId: string) => {
   const db = await ctx.ensureDb();
   const bundle = await ctx.ensureRuntime();

   const doc = db.prepare('SELECT id, path FROM documents WHERE id = ? OR path = ?')
     .get(filePathOrDocId, filePathOrDocId) as { id: string; path: string } | undefined;

   if (!doc) return { success: false, error: 'Document not found' };

   // 1. 删除向量索引（不删原始文件）
   try { await bundle.ragEngine.removeDocument(doc.path); } catch { /* 向量清理失败不阻塞 */ }

   // 2. 删除 chunks（FTS5 触发器自动删除 chunks_fts 对应行）
   db.prepare('DELETE FROM chunks WHERE document_id = ?').run(doc.id);

   // 3. 删除 documents 记录
   db.prepare('DELETE FROM documents WHERE id = ?').run(doc.id);

   db.save();
   return { success: true };
 });

 验收

 - knowledge-remove 后重启不再命中
 - 原始文件保留在磁盘

 ---
 Task 4: 实现真实 Hybrid RAG 检索链路

 4.1 chunks 表现有结构（确认）

 chunks (
   id TEXT PK,
   document_id TEXT FK,
   chunk_index INTEGER,
   content_preview TEXT,   -- 只是预览，非全文
   source_locator TEXT,
   token_count INTEGER DEFAULT 0,
   vector_id TEXT,
   created_at REAL
 )

 没有 content（全文）、source_file 字段。

 4.2 FTS5 迁移（幂等 + 回填）

 新建: packages/store/src/migrations/004_chunks_fts.ts

 关键设计：
 1. 用函数型 migration（如 003 的 addColumnIfNotExists 风格），ALTER TABLE ADD COLUMN 幂等执行
 2. FTS5 不可用时跳过虚拟表和触发器
 3. 创建 FTS5 后回填已有 chunks

 import type { Database } from 'better-sqlite3';

 export const MIGRATION_004_VERSION = 4;

 /**
  * 004 迁移：为 chunks 增加全文列，创建 FTS5 虚拟表，回填已有数据。
  * 必须使用函数型 migration 以支持条件执行和幂等 ALTER。
  */
 export function applyMigration004(
   db: Database,
   log: (msg: string) => void,
   fts5Available: boolean,
 ): void {
   // 1. 幂等增加列
   addColumnIfNotExists(db, 'chunks', 'content', 'TEXT');
   addColumnIfNotExists(db, 'chunks', 'source_file', 'TEXT');
   log('004: Added content, source_file columns to chunks');

   if (!fts5Available) {
     log('004: FTS5 not available, skipping FTS virtual table');
     // 仍然记录版本号，BM25Search 降级到 LIKE
     insertSchemaVersion(db, MIGRATION_004_VERSION);
     return;
   }

   // 2. 创建 FTS5 虚拟表
   db.exec(`
     CREATE VIRTUAL TABLE IF NOT EXISTS chunks_fts USING fts5(
       chunk_id,
       source_file,
       content
     );
   `);

   // 3. 同步触发器
   db.exec(`
     CREATE TRIGGER IF NOT EXISTS chunks_fts_insert AFTER INSERT ON chunks BEGIN
       INSERT INTO chunks_fts(rowid, chunk_id, source_file, content)
       VALUES (new.rowid, new.id, new.source_file, new.content);
     END;

     CREATE TRIGGER IF NOT EXISTS chunks_fts_delete AFTER DELETE ON chunks BEGIN
       INSERT INTO chunks_fts(chunks_fts, rowid, chunk_id, source_file, content)
       VALUES ('delete', old.rowid, old.id, old.source_file, old.content);
     END;

     CREATE TRIGGER IF NOT EXISTS chunks_fts_update AFTER UPDATE ON chunks BEGIN
       INSERT INTO chunks_fts(chunks_fts, rowid, chunk_id, source_file, content)
       VALUES ('delete', old.rowid, old.id, old.source_file, old.content);
       INSERT INTO chunks_fts(rowid, chunk_id, source_file, content)
       VALUES (new.rowid, new.id, new.source_file, new.content);
     END;
   `);

   // 4. 回填已有 chunks（content 从 documents.path 关联）
   db.exec(`
     INSERT OR IGNORE INTO chunks_fts(rowid, chunk_id, source_file, content)
     SELECT c.rowid, c.id, d.path, c.content
     FROM chunks c
     JOIN documents d ON c.document_id = d.id
     WHERE c.content IS NOT NULL;
   `);
   log('004: Created chunks_fts, triggers, and backfilled existing data');

   insertSchemaVersion(db, MIGRATION_004_VERSION);
 }

 /** 幂等添加列 */
 function addColumnIfNotExists(db: Database, table: string, column: string, type: string): void {
   const cols = db.pragma(`table_info(${table})`) as Array<{ name: string }>;
   if (!cols.some(c => c.name === column)) {
     db.exec(`ALTER TABLE ${table} ADD COLUMN ${column} ${type}`);
   }
 }

 function insertSchemaVersion(db: Database, version: number): void {
   db.exec(`INSERT OR IGNORE INTO schema_version (version, applied_at) VALUES (${version}, strftime('%s','now'))`);
 }

 文件: packages/store/src/database.ts — 追加：

 import { MIGRATION_004_VERSION, applyMigration004 } from './migrations/004_chunks_fts.js';

 // migrations 数组追加
 { version: MIGRATION_004_VERSION, fn: applyMigration004 },

 4.3 注入 ChunkMetadataStore 接口（RAG 包不依赖 store）

 文件: packages/rag/src/engine.ts

 /**
  * 块元数据存储接口。
  * 由调用方注入具体实现，避免 RAG 包硬绑定 @workagent/store。
  */
 export interface ChunkMetadataStore {
   /**
    * 批量写入块元数据（全文 + 来源文件路径）。
    * @param chunks - 块元数据列表。
    */
   upsertChunkMetadata(chunks: Array<{
     chunkId: string;
     sourceFile: string;
     content: string;
   }>): void;

   /**
    * 删除指定文档的所有块元数据。
    * @param documentId - 文档 ID。
    */
   deleteChunkMetadata(documentId: string): void;
 }

 ⚠️ 关键修正：DocumentChunk（packages/rag/src/chunker.ts:11）没有 documentId 字段。ChunkMetadataStore.upsertChunkMetadata() 的参数也不应包含 documentId——documentId 是 store 层概念，chunk
 层不需要。

 在 RAGEngine.indexDocument(document, onProgress) 中，document 参数是 ExtractedDocument（有 filePath 等字段），索引时由调用方（runtime-factory / ipc handler）负责将 documentId 传入
 metadata store。

 4.4 RAGEngine 构造函数改为 options object

 文件: packages/rag/src/engine.ts

 /**
  * RAG 引擎配置。
  */
 export interface RAGEngineOptions {
   /** 向量存储后端。 */
   index: KnowledgeIndex;
   /** 嵌入服务。 */
   embedder: OllamaEmbedder;
   /** 文档分块器（可选，默认 DocumentChunker）。 */
   chunker?: DocumentChunker;
   /** 块元数据存储（可选，用于 BM25 检索的全文写入）。 */
   metadataStore?: ChunkMetadataStore;
   /** 可插拔检索组件（可选，用于 hybrid/rerank/rewrite）。 */
   components?: RetrievalComponents;
 }

 export class RAGEngine {
   private pipeline: RetrievalPipeline;

   constructor(options: RAGEngineOptions) {
     const { index, embedder, chunker, metadataStore, components } = options;
     this.pipeline = new RetrievalPipeline(index, embedder, components);
     // ... 存储其他依赖
   }
 }

 调用方改为：new RAGEngine({ index, embedder, metadataStore, components })。

 4.5 BM25Search（注入 queryFn，RetrievedChunk 从 @workagent/shared 导入）

 新建: packages/rag/src/bm25-search.ts

 import type { RetrievedChunk } from '@workagent/shared';

 /**
  * BM25 稀疏检索选项。
  */
 export interface BM25SearchOptions {
   topK?: number;
 }

 /**
  * 基于 SQLite FTS5 的 BM25 稀疏检索。
  * FTS5 不可用时降级为 LIKE 子串匹配。
  * 不直接依赖 @workagent/store，通过注入的 queryFn 查询。
  */
 export class BM25Search {
   private queryFn: (sql: string, params: unknown[]) => Array<Record<string, unknown>>;
   private fts5Available: boolean;

   constructor(
     queryFn: (sql: string, params: unknown[]) => Array<Record<string, unknown>>,
     fts5Available: boolean,
   ) {
     this.queryFn = queryFn;
     this.fts5Available = fts5Available;
   }

   /**
    * 执行关键词检索。
    * @param query - 搜索查询词。空查询返回空结果，避免非法 SQL。
    * @param options - 检索选项。
    * @returns 匹配的文档块列表。
    */
   async search(query: string, options?: BM25SearchOptions): Promise<RetrievedChunk[]> {
     if (!query || !query.trim()) return [];

     const topK = options?.topK ?? 10;

     if (this.fts5Available) {
       return this.searchFTS5(query, topK);
     }
     return this.searchLIKE(query, topK);
   }

   private searchFTS5(query: string, topK: number): RetrievedChunk[] {
     try {
       const rows = this.queryFn(
         `SELECT c.id as chunk_id, c.source_file, c.content,
                 bm25(chunks_fts) as rank
          FROM chunks_fts f JOIN chunks c ON f.chunk_id = c.id
          WHERE chunks_fts MATCH ?
          ORDER BY rank LIMIT ?`,
         [query, topK],
       );

       return rows.map((row) => ({
         chunkId: String(row.chunk_id),
         sourceFile: String(row.source_file ?? ''),
         sourceType: 'unknown',
         locator: '',
         content: String(row.content ?? ''),
         score: 1 / (1 + Math.abs(Number(row.rank))),
       }));
     } catch {
       // FTS5 查询失败（如特殊字符），降级到 LIKE
       return this.searchLIKE(query, topK);
     }
   }

   private searchLIKE(query: string, topK: number): RetrievedChunk[] {
     const terms = query.split(/\s+/).filter(Boolean);
     if (terms.length === 0) return [];

     const likeClauses = terms.map(() => `content LIKE ?`).join(' OR ');
     const params = terms.map(t => `%${t}%`);

     const rows = this.queryFn(
       `SELECT id as chunk_id, source_file, content
        FROM chunks WHERE content IS NOT NULL AND (${likeClauses}) LIMIT ?`,
       [...params, topK],
     );

     return rows.map((row) => ({
       chunkId: String(row.chunk_id),
       sourceFile: String(row.source_file ?? ''),
       sourceType: 'unknown',
       locator: '',
       content: String(row.content ?? ''),
       score: 0.3,
     }));
   }
 }

 4.6 RRF 混合融合（与上一版相同，此处省略）

 4.7 QueryRewriter / Reranker / RelevanceGrader（与上一版相同，此处省略）

 关键要点：
 - BGEReranker 最多处理 20 条结果，超出直接追加
 - 3 次连续失败后自动禁用
 - 所有接口中 RetrievedChunk 从 @workagent/shared 导入

 4.8 runtime-factory 注入所有组件

 文件: apps/desktop/electron/runtime-factory.ts

 // ChunkMetadataStore 注入（不含 documentId，documentId 由 ipc handler 在 documents 表管理）
 const chunkMetadataStore: ChunkMetadataStore = {
   upsertChunkMetadata(chunks) {
     const stmt = db.prepare(
       `INSERT OR REPLACE INTO chunks (id, document_id, chunk_index, content_preview, source_locator, content, source_file, token_count, created_at)
        VALUES (?, ?, 0, ?, '', ?, ?, 0, strftime('%s','now'))`,
     );
     for (const c of chunks) {
       // documentId 从外部上下文获取（如 ipc handler 传入）
       stmt.run(c.chunkId, c.documentId ?? '', c.content.slice(0, 100), c.content, c.sourceFile);
     }
     db.save();
   },
   deleteChunkMetadata(documentId) {
     db.prepare('DELETE FROM chunks WHERE document_id = ?').run(documentId);
     db.save();
   },
 };

 // BM25Search 注入 queryFn
 const bm25QueryFn = (sql: string, params: unknown[]) => {
   return db.prepare(sql).all(...params) as Array<Record<string, unknown>>;
 };
 const bm25Search = new BM25Search(bm25QueryFn, /* fts5Available from db */ true);

 const ragEngine = new RAGEngine({
   index: vectorStore,
   embedder: new OllamaEmbedder(modelProvider),
   metadataStore: chunkMetadataStore,
   components: {
     sparseSearcher: bm25Search,
     queryRewriter: new RuleBasedQueryRewriter(),
     reranker: new PassThroughReranker(),
     relevanceGrader: new ScoreAndKeywordGrader(),
   },
 });

 4.9 RAG 质量测试（mock dense miss 证明 hybrid fallback）

 新建: tests/e2e/rag-quality.test.ts

 describe('RAG quality regression', () => {
   it('hybrid hits exact policy numbers when dense returns empty', async () => {
     // 1. mock dense index 返回空 []（模拟 embedding 无法匹配精确文号）
     // 2. mock BM25Search 返回含"国发〔2024〕3号"的 chunk
     // 3. 调用 pipeline.retrieve()
     // 4. 断言 hybrid 结果命中
     // 5. 真实 Ollama 测试在 RUN_REAL_RAG=1 时运行，非阻塞
   });

   it('RRF boosts chunks appearing in both dense and sparse', async () => {
     // dense 返回 chunk A (score=0.8)
     // sparse 返回 chunk A (rank=1) + chunk B (rank=2)
     // RRF 后 chunk A 排名应高于 chunk B
   });
 });

 验收

 - RAGEngine.search() 内部走 RetrievalPipeline
 - 精确政策文号通过 sparse 命中
 - 无 reranker 模型时不崩溃
 - FTS5 不可用时 LIKE fallback
 - 空查询不产生非法 SQL
 - @workagent/rag 不新增 @workagent/store 依赖
 - 004 migration 幂等执行 + 回填已有 chunks

 ---
 Task 5: 补 Query Rewrite/Reranker/Grader 可降级实现

 （与上一版相同，核心要点：）

 - OllamaQueryRewriter 降级为 RuleBasedQueryRewriter
 - BGEReranker 最多 20 条 + 3 次失败禁用
 - runtime-factory 根据 Ollama 可用性组装组件
 - isModelAvailable 使用精确匹配 + alias 白名单

 ---
 Task 6: 实现 transcript 恢复、drain collapse、withheld retry

 6.1 drainCollapse（与上一版相同）

 6.2 runtime.ts handleError 增加 drain collapse 路径（与上一版相同）

 6.3 resumeSession（通过 agent_runs 找 runId，注入 RunLookupStore）

 新建: packages/agent-core/src/context/resume-session.ts

 关键修正：
 1. TranscriptStore 按 runId.jsonl 写入，不是 sessionId.jsonl
 2. 通过注入的 RunLookupStore 从 agent_runs 表查最新 runId
 3. 恢复最新 run 快照，UI 标记 terminal status

 /**
  * 数据库查询接口（注入，避免依赖 @workagent/store）。
  */
 export interface RunLookupStore {
   /**
    * 查找指定 session 最新的 run ID 及状态。
    * @param sessionId - 会话 ID。
    * @returns 最新 run 的 ID 和状态，或 null。
    */
   getLatestRun(sessionId: string): { runId: string; status: string; terminalReason: string | null } | null;
 }

 /**
  * 从 transcript JSONL 恢复会话状态快照。
  * 恢复最新 run 快照，UI 标记 terminal status。
  *
  * @param sessionId - 会话 ID。
  * @param transcriptDir - transcript 存储目录。
  * @param runLookup - 数据库查询接口。
  * @returns 恢复的状态快照，或 null。
  */
 export function resumeSession(
   sessionId: string,
   transcriptDir: string,
   runLookup: RunLookupStore,
 ): {
   runId: string;
   lastSequence: number;
   terminalStatus: string | null;  // UI 用此标记是否需要人工确认
   lastAssistantContent: string;
   activePlanSnapshot: Record<string, unknown> | null;
 } | null {
   // 1. 找最新 run
   const latestRun = runLookup.getLatestRun(sessionId);
   if (!latestRun) return null;

   // 2. 读 JSONL
   const filePath = path.join(transcriptDir, `${latestRun.runId}.jsonl`);
   if (!fs.existsSync(filePath)) return null;

   // 3. 解析（同上一版）
   // ...

   return {
     runId: latestRun.runId,
     lastSequence,
     terminalStatus: latestRun.status,  // 'running'|'failed'|'aborted'|'completed'
     lastAssistantContent,
     activePlanSnapshot,
   };
 }

 runtime-factory 注入 RunLookupStore：

 const runLookup: RunLookupStore = {
   getLatestRun(sessionId: string) {
     const row = db.prepare(
       'SELECT id, status, terminal_reason FROM agent_runs WHERE session_id = ? ORDER BY started_at DESC LIMIT 1',
     ).get(sessionId) as { id: string; status: string; terminal_reason: string | null } | undefined;
     return row ? { runId: row.id, status: row.status, terminalReason: row.terminal_reason } : null;
   },
 };

 验收

 - context overflow 三步恢复链：reactive → drain → graceful stop
 - 瞬态错误重试 2 次不展示
 - resumeSession 通过 agent_runs 找 runId 再读 JSONL
 - UI 能看到 terminal status（running/failed/aborted/completed）
 - @workagent/agent-core 不直接依赖 @workagent/store

 ---
 Task 7: 补 fixtures、RAG 质量回归和真实集成测试

 （与上一版基本相同，核心补充：）

 - Electron smoke 只在 RUN_ELECTRON_SMOKE=1 或专门 CI job 运行，不纳入 verify 门禁
 - test:electron 脚本加环境检查：

 {
   "test:electron": "RUN_ELECTRON_SMOKE=1 npx playwright test --config tests/electron-smoke/playwright.config.ts",
   "verify": "pnpm doctor:env && pnpm test:unit && pnpm test:e2e && pnpm --filter @workagent/desktop build"
 }

 ---
 Task 8: 最终验收门禁

 （与上一版相同）

 ---
 修正对照表（全部 15 条）

 ┌─────┬───────────────────────────────────────────────┬───────────────────────────────────────────────────┐
 │  #  │                  原计划问题                   │                       修正                        │
 ├─────┼───────────────────────────────────────────────┼───────────────────────────────────────────────────┤
 │ 1   │ doctor-env 引用 dist/constants.js             │ 新建 model-constants.cjs 无构建依赖文件           │
 ├─────┼───────────────────────────────────────────────┼───────────────────────────────────────────────────┤
 │ 2   │ isModelAvailable tag 前缀匹配有误判           │ 精确匹配 + alias 白名单                           │
 ├─────┼───────────────────────────────────────────────┼───────────────────────────────────────────────────┤
 │ 3   │ Worker abort 缺 currentSessionId/currentRunId │ handleChat 开始时保存，finally 清空               │
 ├─────┼───────────────────────────────────────────────┼───────────────────────────────────────────────────┤
 │ 4   │ abortEmitted 布尔值并发/延迟问题              │ 按 runId 维护 terminalRunIds Set                  │
 ├─────┼───────────────────────────────────────────────┼───────────────────────────────────────────────────┤
 │ 5   │ onEvent 多 listener 注册泄漏                  │ 返回 unsubscribe，runTurn finally 调用            │
 ├─────┼───────────────────────────────────────────────┼───────────────────────────────────────────────────┤
 │ 6   │ approvePlan 后直接 executing                  │ approved 保持，下次 runTurn 检测后 startExecution │
 ├─────┼───────────────────────────────────────────────┼───────────────────────────────────────────────────┤
 │ 7   │ ALTER TABLE 非幂等                            │ addColumnIfNotExists 函数型 migration             │
 ├─────┼───────────────────────────────────────────────┼───────────────────────────────────────────────────┤
 │ 8   │ chunks_fts 无回填                             │ 迁移后 INSERT FROM chunks 回填                    │
 ├─────┼───────────────────────────────────────────────┼───────────────────────────────────────────────────┤
 │ 9   │ DocumentChunk 无 documentId                   │ ChunkMetadataStore 参数不含 documentId            │
 ├─────┼───────────────────────────────────────────────┼───────────────────────────────────────────────────┤
 │ 10  │ RetrievedChunk 从 ./types 导入                │ 从 @workagent/shared 导入                         │
 ├─────┼───────────────────────────────────────────────┼───────────────────────────────────────────────────┤
 │ 11  │ RAGEngine 构造函数堆 positional 参数          │ 改为 options object                               │
 ├─────┼───────────────────────────────────────────────┼───────────────────────────────────────────────────┤
 │ 12  │ Electron smoke 混入 verify 门禁               │ RUN_ELECTRON_SMOKE=1 独立运行                     │
 ├─────┼───────────────────────────────────────────────┼───────────────────────────────────────────────────┤
 │ 13  │ knowledge-remove 可能删原始文件               │ 明确只删 DB+向量，不删磁盘文件                    │
 ├─────┼───────────────────────────────────────────────┼───────────────────────────────────────────────────┤
 │ 14  │ RAG 质量测试 dense miss 难构造                │ mock dense 返回空，证明 hybrid fallback           │
 ├─────┼───────────────────────────────────────────────┼───────────────────────────────────────────────────┤
 │ 15  │ resumeSession 取哪个 run                      │ 最新 run 快照 + UI 标记 terminal status           │
 └─────┴───────────────────────────────────────────────┴───────────────────────────────────────────────────┘

 关键文件索引

 ┌───────────────────────────────────────────────────┬───────────┬──────────────────────────────────────────────────────┐
 │                       文件                        │ 涉及 Task │                       改动类型                       │
 ├───────────────────────────────────────────────────┼───────────┼──────────────────────────────────────────────────────┤
 │ packages/shared/model-constants.cjs               │ 1         │ 新建（无构建依赖）                                   │
 ├───────────────────────────────────────────────────┼───────────┼──────────────────────────────────────────────────────┤
 │ packages/shared/src/constants.ts                  │ 1         │ 新增模型常量+isModelAvailable                        │
 ├───────────────────────────────────────────────────┼───────────┼──────────────────────────────────────────────────────┤
 │ packages/ingest/package.json                      │ 1         │ 新增 test 脚本                                       │
 ├───────────────────────────────────────────────────┼───────────┼──────────────────────────────────────────────────────┤
 │ packages/rag/package.json                         │ 1         │ 新增 test 脚本                                       │
 ├───────────────────────────────────────────────────┼───────────┼──────────────────────────────────────────────────────┤
 │ packages/tools/package.json                       │ 1         │ 新增 test 脚本                                       │
 ├───────────────────────────────────────────────────┼───────────┼──────────────────────────────────────────────────────┤
 │ scripts/doctor-env.js                             │ 1         │ 引用 CJS 常量+模型精确检查                           │
 ├───────────────────────────────────────────────────┼───────────┼──────────────────────────────────────────────────────┤
 │ package.json                                      │ 1,7       │ overrides + 新脚本                                   │
 ├───────────────────────────────────────────────────┼───────────┼──────────────────────────────────────────────────────┤
 │ packages/agent-core/src/plan-controller.ts        │ 2         │ 多 listener + unsubscribe                            │
 ├───────────────────────────────────────────────────┼───────────┼──────────────────────────────────────────────────────┤
 │ packages/agent-core/src/runtime.ts                │ 2,6       │ execute启动 + drain + withheld + onEvent unsubscribe │
 ├───────────────────────────────────────────────────┼───────────┼──────────────────────────────────────────────────────┤
 │ packages/agent-core/src/query-state.ts            │ 6         │ hasAttemptedDrainCollapse + withheldRetryCount       │
 ├───────────────────────────────────────────────────┼───────────┼──────────────────────────────────────────────────────┤
 │ apps/desktop/electron/workers/agent-worker.ts     │ 2         │ 保存 currentSessionId/currentRunId + abort事件       │
 ├───────────────────────────────────────────────────┼───────────┼──────────────────────────────────────────────────────┤
 │ apps/desktop/electron/worker-bridge.ts            │ 2         │ abort 事件                                           │
 ├───────────────────────────────────────────────────┼───────────┼──────────────────────────────────────────────────────┤
 │ apps/desktop/electron/ipc-handlers.ts             │ 2,3       │ 拆分 + terminalRunIds                                │
 ├───────────────────────────────────────────────────┼───────────┼──────────────────────────────────────────────────────┤
 │ apps/desktop/src/components/ConversationPane.tsx  │ 2         │ 审批按钮(从sessionStore取sessionId)                  │
 ├───────────────────────────────────────────────────┼───────────┼──────────────────────────────────────────────────────┤
 │ apps/desktop/electron/preload.ts                  │ 2,3       │ 新增 API                                             │
 ├───────────────────────────────────────────────────┼───────────┼──────────────────────────────────────────────────────┤
 │ apps/desktop/electron/ipc/context.ts              │ 3         │ 新建                                                 │
 ├───────────────────────────────────────────────────┼───────────┼──────────────────────────────────────────────────────┤
 │ apps/desktop/electron/ipc/chat-ipc.ts             │ 3         │ 新建                                                 │
 ├───────────────────────────────────────────────────┼───────────┼──────────────────────────────────────────────────────┤
 │ apps/desktop/electron/ipc/knowledge-ipc.ts        │ 3         │ 新建(含knowledge-remove，只删DB+向量)                │
 ├───────────────────────────────────────────────────┼───────────┼──────────────────────────────────────────────────────┤
 │ apps/desktop/electron/ipc/settings-ipc.ts         │ 3         │ 新建                                                 │
 ├───────────────────────────────────────────────────┼───────────┼──────────────────────────────────────────────────────┤
 │ packages/store/src/migrations/004_chunks_fts.ts   │ 4         │ 新建(幂等ALTER+FTS5+回填)                            │
 ├───────────────────────────────────────────────────┼───────────┼──────────────────────────────────────────────────────┤
 │ packages/store/src/database.ts                    │ 4         │ 追加函数型迁移                                       │
 ├───────────────────────────────────────────────────┼───────────┼──────────────────────────────────────────────────────┤
 │ packages/rag/src/bm25-search.ts                   │ 4         │ 新建(注入queryFn, RetrievedChunk从shared导入)        │
 ├───────────────────────────────────────────────────┼───────────┼──────────────────────────────────────────────────────┤
 │ packages/rag/src/hybrid-fusion.ts                 │ 4         │ 新建(RRF)                                            │
 ├───────────────────────────────────────────────────┼───────────┼──────────────────────────────────────────────────────┤
 │ packages/rag/src/query-rewriter.ts                │ 4,5       │ 新建                                                 │
 ├───────────────────────────────────────────────────┼───────────┼──────────────────────────────────────────────────────┤
 │ packages/rag/src/reranker.ts                      │ 4,5       │ 新建(最多20条+3次禁用)                               │
 ├───────────────────────────────────────────────────┼───────────┼──────────────────────────────────────────────────────┤
 │ packages/rag/src/relevance-grader.ts              │ 4,5       │ 新建                                                 │
 ├───────────────────────────────────────────────────┼───────────┼──────────────────────────────────────────────────────┤
 │ packages/rag/src/retrieval-pipeline.ts            │ 4         │ 重构(可插拔组件)                                     │
 ├───────────────────────────────────────────────────┼───────────┼──────────────────────────────────────────────────────┤
 │ packages/rag/src/engine.ts                        │ 4         │ options object + metadataStore + pipeline            │
 ├───────────────────────────────────────────────────┼───────────┼──────────────────────────────────────────────────────┤
 │ packages/rag/src/index.ts                         │ 4         │ re-export                                            │
 ├───────────────────────────────────────────────────┼───────────┼──────────────────────────────────────────────────────┤
 │ apps/desktop/electron/runtime-factory.ts          │ 4,5       │ 注入所有组件                                         │
 ├───────────────────────────────────────────────────┼───────────┼──────────────────────────────────────────────────────┤
 │ packages/agent-core/src/context/drain-collapse.ts │ 6         │ 新建                                                 │
 ├───────────────────────────────────────────────────┼───────────┼──────────────────────────────────────────────────────┤
 │ packages/agent-core/src/context/resume-session.ts │ 6         │ 新建(通过agent_runs+RunLookupStore)                  │
 ├───────────────────────────────────────────────────┼───────────┼──────────────────────────────────────────────────────┤
 │ packages/agent-core/src/context/pipeline.ts       │ 6         │ drain collapse 集成                                  │
 ├───────────────────────────────────────────────────┼───────────┼──────────────────────────────────────────────────────┤
 │ tests/fixtures/knowledge/*.txt                    │ 7         │ 新建                                                 │
 ├───────────────────────────────────────────────────┼───────────┼──────────────────────────────────────────────────────┤
 │ tests/fixtures/rag-eval.json                      │ 7         │ 新建                                                 │
 ├───────────────────────────────────────────────────┼───────────┼──────────────────────────────────────────────────────┤
 │ tests/e2e/rag-quality.test.ts                     │ 7         │ 新建(mock dense miss)                                │
 ├───────────────────────────────────────────────────┼───────────┼──────────────────────────────────────────────────────┤
 │ tests/electron-smoke/smoke.test.ts                │ 7         │ 新建(独立Playwright, RUN_ELECTRON_SMOKE=1)           │
 └───────────────────────────────────────────────────┴───────────┴──────────────────────────────────────────────────────┘

 现有可复用函数/工具

 ┌──────────────────────────────────┬──────────────────────────────────────────────────────┬─────────────────────────────────────┐
 │             函数/类              │                         位置                         │                用途                 │
 ├──────────────────────────────────┼──────────────────────────────────────────────────────┼─────────────────────────────────────┤
 │ updateQueryLoopState()           │ agent-core/src/query-state.ts                        │ 不可变状态更新                      │
 ├──────────────────────────────────┼──────────────────────────────────────────────────────┼─────────────────────────────────────┤
 │ reactiveCompact()                │ agent-core/src/context/compact.ts                    │ 溢出时 LLM 摘要压缩                 │
 ├──────────────────────────────────┼──────────────────────────────────────────────────────┼─────────────────────────────────────┤
 │ autoCompactIfNeeded()            │ agent-core/src/context/compact.ts                    │ 80% 阈值自动压缩 + 熔断器           │
 ├──────────────────────────────────┼──────────────────────────────────────────────────────┼─────────────────────────────────────┤
 │ estimateTokens()                 │ agent-core/src/context/budget.ts                     │ 字符级 token 估算                   │
 ├──────────────────────────────────┼──────────────────────────────────────────────────────┼─────────────────────────────────────┤
 │ compactRecovery()                │ agent-core/src/context/compact-recovery.ts           │ compact 后 RAG/memory/plan 恢复注入 │
 ├──────────────────────────────────┼──────────────────────────────────────────────────────┼─────────────────────────────────────┤
 │ TranscriptStore                  │ agent-core/src/context/transcript.ts                 │ JSONL 按 runId 追加写入             │
 ├──────────────────────────────────┼──────────────────────────────────────────────────────┼─────────────────────────────────────┤
 │ SessionMemoryPersist             │ agent-core/src/context/session-memory-persist.ts     │ .md 持久化                          │
 ├──────────────────────────────────┼──────────────────────────────────────────────────────┼─────────────────────────────────────┤
 │ createDesktopRuntimeBundle()     │ apps/desktop/electron/runtime-factory.ts             │ 统一装配                            │
 ├──────────────────────────────────┼──────────────────────────────────────────────────────┼─────────────────────────────────────┤
 │ createRuntimeBundleForTest()     │ apps/desktop/electron/runtime-factory.ts             │ 测试用快照                          │
 ├──────────────────────────────────┼──────────────────────────────────────────────────────┼─────────────────────────────────────┤
 │ reconcileDeletedKnowledge()      │ apps/desktop/electron/runtime-factory.ts             │ 清理已删除知识                      │
 ├──────────────────────────────────┼──────────────────────────────────────────────────────┼─────────────────────────────────────┤
 │ OllamaEmbedder                   │ packages/rag/src/embedder.ts                         │ Ollama embedding                    │
 ├──────────────────────────────────┼──────────────────────────────────────────────────────┼─────────────────────────────────────┤
 │ LanceDBVectorStore               │ packages/rag/src/lancedb-store.ts                    │ 向量存储                            │
 ├──────────────────────────────────┼──────────────────────────────────────────────────────┼─────────────────────────────────────┤
 │ MemoryVectorStore                │ packages/rag/src/memory-store.ts                     │ 内存降级存储                        │
 ├──────────────────────────────────┼──────────────────────────────────────────────────────┼─────────────────────────────────────┤
 │ DocumentChunker                  │ packages/rag/src/chunker.ts                          │ 字符分块                            │
 ├──────────────────────────────────┼──────────────────────────────────────────────────────┼─────────────────────────────────────┤
 │ IngestPipeline.checkIdempotent() │ packages/ingest/src/pipeline.ts                      │ SHA-256 幂等检查                    │
 ├──────────────────────────────────┼──────────────────────────────────────────────────────┼─────────────────────────────────────┤
 │ agent_runs 表                    │ packages/store/src/migrations/003_workspaces_runs.ts │ session→runId 映射                  │
 ├──────────────────────────────────┼──────────────────────────────────────────────────────┼─────────────────────────────────────┤
 │ addColumnIfNotExists 模式        │ packages/store/src/migrations/003_workspaces_runs.ts │ 幂等 ALTER TABLE                    │
 ├──────────────────────────────────┼──────────────────────────────────────────────────────┼─────────────────────────────────────┤
 │ useSessionStore                  │ apps/desktop/src/stores/session-store.ts             │ 当前 sessionId (currentSessionId)   │
 └──────────────────────────────────┴──────────────────────────────────────────────────────┴─────────────────────────────────────┘
